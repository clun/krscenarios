Snitches Quiz

>>1. Snitch is used to: <<
( ) Communicate with other nodes.
( ) Assign a master node in cluster.
(*) Determine/declare each node's rack and data center.
( ) Report downed nodes to coordinator node.

>>2. Snitch is configured in the cassandra.yaml file.<<
(*) TRUE 
( ) FALSE 

>>3. Which is the following is _not_ a type of snitch?<<
( ) `SimpleSnitch`
( ) `PropertyFileSnitch`
( ) `DynamicSnitch`
(*) `CassandraSnitch`